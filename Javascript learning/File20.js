// While loop

let i = 0;

while(i<=10)
{
    console.log(i);
    i++;
}


// First while loop checks condition
// condition can be:

// True 
// False

// if false : variable exits loop

// if True : variable prints its value via console.log(i);

// i++ increased the value of variable by 1 and further the value of variable gets checked in the condition until the condition gets false
